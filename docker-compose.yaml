version: '3.8'

services:
  r3-pipeline:
    build: .
    container_name: r3_pipeline_service
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - ENVIRONMENT=production